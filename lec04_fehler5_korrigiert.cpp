#include <iostream>
#include <cmath>

using namespace std;

int main() {
    std::cout << "sin(pi/4) ist " << sin(3.141592653 / 4.0) << std::endl;
    std::cout << "sqrt(2)/2 ist " << sqrt(2.0) / 2.0 << std::endl;

    return 0;
}
